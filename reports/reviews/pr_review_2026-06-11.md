# PR Review — 2026-06-11

Loop: PRReview (Fable 5) — repo pubblica `SandroHub013/UnaBetting`.

## Run 16:30 UTC

| PR | Titolo | Verdetto | Esito |
|----|--------|----------|-------|
| #2 | Implement E2: Walk-forward ensemble weighting | ✅ ACCEPTED | MERGED (squash, branch eliminato) |

### PR #2 — dettaglio

**Cosa fa**: `PreFittedEnsemble` passa da media piatta a media pesata; pesi = softmax
stabile su -log-loss di validation, calcolati in `train_models` (E2 di EXPERIMENTS.md).
Include anche fix test stale E0 (commit a9142df nel branch).

**Verifiche**:
- **pytest**: 43 passed, 2 skipped (CSV features non buildato — ambientale).
  Unico fail `test_file_api_rejects_binary_and_missing` (404 vs 415) riprodotto
  identico su `main`: pre-esistente, `models/*.pkl` non in git. Non causato dalla PR.
- **Secrets/dati personali**: nessuno nel diff.
- **Anti-leak (CONTRIBUTING)**: pesi solo da validation set, fallback a pesi uniformi
  se `has_val=False`; nessun uso del test set. OK.
- **Math smoke test**: `predict_proba` media pesata su axis 0 corretta; normalizzazione
  pesi corretta; `predict` threshold corretto. Fix test E0 corretto: `res[-2]` combacia
  con la tupla di ritorno reale di `prepare_training_data` (`..., medians,
  player_mapping`) — il vecchio `*_, medians` prendeva `player_mapping`.

**Conflitto risolto**: branch PR era dietro `main` (commit DocsSync). Conflitto solo
su `EXPERIMENTS.md` (voce E0 + nuova E0b da main vs `[x]` su testo vecchio): tenuta
versione main, merge commit f1b2b75 pushato sul branch PR, test ri-eseguiti verdi,
poi squash-merge.

**Note non bloccanti**: pickle vecchi di `PreFittedEnsemble` non hanno l'attributo
`weights`; un re-train li rigenera.

**Azioni GitHub**: `--approve` bloccato (PR own-account, atteso) → commento
✅ ACCEPTED + label `loop-accepted` + squash-merge alle 16:35 UTC.

## Run 20:35 UTC

| PR | Titolo | Verdetto | Esito |
|----|--------|----------|-------|
| #3 | Implement E3: XGBoost sigmoid calibration | ✅ ACCEPTED | MERGED (squash, branch eliminato) |

### PR #3 — dettaglio

**Cosa fa**: `_calibrate_classifier` accetta parametro `method` (default `isotonic`);
solo XGBoost passa a `method="sigmoid"` per evitare l'overfit isotonico su val=[2024]
(E3 di EXPERIMENTS.md, LL 0.61→0.66). Il branch includeva anche E2 ed E0, già
mergiati via PR #2/#1 — dopo il merge con main il delta netto era solo E3.

**Verifiche**:
- **pytest**: 43 passed, 2 skipped (CSV features non buildato — ambientale). Unico
  fail `test_file_api_rejects_binary_and_missing` (404 vs 415) riprodotto identico su
  `main`: pre-esistente, non causato dalla PR. Suite ri-eseguita verde anche dopo la
  risoluzione del conflitto.
- **Secrets/dati personali**: nessuno nel diff.
- **Anti-leak (CONTRIBUTING)**: calibrazione sigmoid su validation set only
  (`cv="prefit"` + fit su X_val), split temporale intatto; LR/RF restano isotonic.
  Nessun claim di accuratezza senza numeri nel body. OK.
- **Import check**: `log_loss` già importato in train.py (riga 18). OK.

**Conflitto risolto**: PR #2 aveva già portato E2 su main → `mergeable: CONFLICTING`.
`train.py` auto-merge pulito (hunks E2 identici); conflitto solo su `EXPERIMENTS.md`
(voce E0): tenuta versione main (nota DONE via PR #1 + voce E0b). Merge commit
6e008be pushato sul branch PR, test verdi, poi squash-merge alle 20:35 UTC.

**Note non bloccanti** (segnalata in commento PR): pickle `PreFittedEnsemble`
pre-E2 non hanno `weights` → `AttributeError` su `predict_proba`; re-train li
rigenera, oppure fallback `getattr` in follow-up.

**Azioni GitHub**: `--approve` bloccato (PR own-account, atteso) → commento
✅ ACCEPTED + label `loop-accepted` + squash-merge.

## Totale giornata: 2 PR esaminate, 2 merged, 0 changes-requested.
