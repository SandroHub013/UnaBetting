# PR Review — 2026-06-12

Loop: PRReview (Fable 5) — repo pubblica `SandroHub013/UnaBetting`.

## Run 1

| PR | Titolo | Verdetto | Esito |
|----|--------|----------|-------|
| #4 | Implement E4: Odds-segment specialist | ❌ CHANGES REQUESTED | OPEN, label `loop-changes-requested` |

### PR #4 — dettaglio

**Cosa fa**: segmenta la pipeline in modello "odds" (righe `has_odds==1`) e modello
"blind" (resto); `train_models` accetta `segment` e filtra il DataFrame prima dello
split; tutti gli artifact salvati con suffisso `_{segment}`; `predict_live` instrada
sul bundle odds/blind in base alla presenza di `odds_p1`/`odds_p2`. Il branch
includeva anche E0/E2/E3 già mergiati — dopo merge con main il delta netto è solo E4.

**Verifiche OK**:
- **pytest**: 43 passed, 2 skipped (features CSV non buildato — ambientale). Unico
  fail `test_file_api_rejects_binary_and_missing` (404 vs 415): pre-esistente,
  identico su main, escluso da regole loop.
- **Secrets/dati personali**: nessuno nel diff.
- **Anti-leak (CONTRIBUTING)**: filtro segmento PRIMA dello split temporale (split
  per anno intatto), mediane train-only per segmento, nessun uso del test set. OK
  a livello codice.
- **Scope**: coerente con E4 di EXPERIMENTS.md.

**Motivi del rifiuto (3 bloccanti)**:
1. **Rinomina artifact rompe i consumer esistenti in silenzio**: `train.py` scrive
   ormai SOLO path suffissati e `__main__` allena solo odds+blind → dopo il prossimo
   retrain `atp_metrics.json`, `atp_scaler.pkl`, `atp_medians.pkl`, `atp_features.txt`
   non vengono più aggiornati, ma li leggono `backtest.py` (valutazione onesta da
   EXPERIMENTS.md), `dashboard/data_api.py:392`, `ui/app.py:39`,
   `scripts/log_metrics_history.py`, `live/inference.py`. Desync silenzioso.
2. **Routing inference morto all'arrivo**: `load_resources` cerca
   `atp_ensemble_{segment}.pkl` ma train salva `atp_target_ensemble_{segment}.pkl`
   (chiave `f"{target_col}_ensemble"`) → FileNotFoundError. Mismatch pre-esistente su
   main, ma lo scopo dichiarato della PR è il routing: deve caricare davvero. In più
   l'artifact è un bundle dict `{"model", "feature_cols"}` mai spacchettato.
3. **E4 marcato [x] senza numeri**: EXPERIMENTS.md richiede risultato registrato e
   keep solo se batte la baseline (66.28% / LL 0.6080 / ROC 0.7312); nessuna metrica
   per segmento riportata, e la rimozione del run "all" da `__main__` elimina il
   modello headline confrontabile.

**Housekeeping**: branch in conflitto con main (blocco E0/E0b in EXPERIMENTS.md);
risolto tenendo la versione main del blocco E0 + E4 [x] della PR, merge commit
7aede45 pushato sul branch → diff vs main ora pulito (solo delta E4).

**Azioni GitHub**: `--request-changes` bloccato (PR own-account, atteso) → commento
❌ NOT accepted (issuecomment-4685680083) + label `loop-changes-requested` (creata,
colore C0392B). PR lasciata OPEN.

## Totale giornata: 1 PR esaminata, 0 merged, 1 changes-requested.
