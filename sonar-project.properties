# SonarQube / GitRoll analysis settings.
#
# e1 — python:S5655 ("Change this argument; Function "byref" expects a different
# type") on src/dashboard/data_api.py is a false positive. `ctypes` and
# `ctypes.wintypes` are imported lazily inside the screenshot handler, so the
# analyzer cannot resolve `ctypes.wintypes.POINT` and does not see that `pt` is a
# ctypes.Structure. Passing it as `ctypes.byref(pt)` is exactly what
# user32.ClientToScreen requires; rewriting it would break the call.
sonar.issue.ignore.multicriteria=e1
sonar.issue.ignore.multicriteria.e1.ruleKey=python:S5655
sonar.issue.ignore.multicriteria.e1.resourceKey=src/dashboard/data_api.py
